# TOOLS — Local tools and conventions

Notes for the agent about local tools, scripts, or conventions. Does not control which tools are available; use the gateway/skills config for that.

Example: “Run integration tests with `npm run test` from the project root.”
